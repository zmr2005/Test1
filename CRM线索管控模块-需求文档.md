# CRM 线索管控模块 — 需求规格说明书（PRD）

## 1. 文档信息

| 项目   | 内容                    |
| ---- | --------------------- |
| 文档名称 | CRM 线索管控模块需求规格说明书     |
| 模块编号 | 板块 3：客户管理（CRM 线索管控模块） |
| 文档版本 | v1.0                  |
| 编制日期 | 2026-09-02            |
| 文档状态 | 评审稿                   |
| 难度等级 | 低                     |

***

## 2. 项目概述

### 2.1 背景

销售业务中，客户线索来源分散（官网、广告、展会、社媒、转介绍等），销售跟进过程缺乏统一管理，存在线索流失、客户撞单、跟进无记录、商机推进不透明、团队工作难以量化等问题。为规范销售过程、沉淀客户资产、提升成交效率，建设 CRM 线索管控模块。

### 2.2 目标

1. 将多渠道销售线索统一汇入并集中管理，避免线索散落与流失。
2. 建立客户归属与防撞客机制，保障销售利益、盘活沉睡客户。
3. 沉淀客户全生命周期资料与跟进记录，形成可回溯的客户资产。
4. 将采购咨询商机纳入规范化阶段管理，提升成交率与过程透明度。
5. 实现团队工作日志与计划的日历化管理，支撑管理者监督与调度。

### 2.3 范围

**范围内**：线索池、客户池、客户档案、商机管理、日程管理五大功能模块及其基础权限控制。

**范围外（本期不含）**：合同/订单管理、回款/开票、营销活动自动化、BI 报表平台、移动端 App、短信/邮件群发平台。

### 2.4 术语定义

| 术语              | 说明                   |
| --------------- | -------------------- |
| 线索（Lead）        | 尚未确认购买意向的潜在客户原始信息    |
| 客户（Customer）    | 已确认具备意向、进入跟进阶段的企业或个人 |
| 商机（Opportunity） | 客户产生的具体采购咨询与成交机会     |
| 公海池             | 存放长期未跟进、可被其他销售认领的客户池 |
| 私有客户            | 有明确负责人、处于跟进保护期的客户    |
| 撞客              | 两名及以上销售同时对同一客户进行跟进   |
| 跟进日志            | 销售对客户/商机每一次沟通的记录     |

***

## 3. 用户角色与权限

| 角色    | 权限说明                                 |
| ----- | ------------------------------------ |
| 销售    | 管理自己的线索/客户/商机；维护跟进记录；填写日报与待办；认领公海客户  |
| 销售主管  | 查看、分配团队数据；查看成员工作计划与完成进度；跟进时效监督       |
| 系统管理员 | 全局配置（标签、来源、商机阶段、公海规则、提醒阈值）；数据导出与权限分配 |

**数据权限模型**：按「负责人 + 所属团队 + 公海可见性」三层控制。销售仅可见本人负责数据及公海数据；主管可见团队数据；管理员可见全量数据。

***

## 4. 功能需求

> 优先级约定：**P0** 核心必需 / **P1** 重要 / **P2** 增强。

## 4.1 线索池

### 4.1.1 线索采集与统一汇入（P0）

**功能描述**：所有渠道采集到的销售线索统一汇入线索池，形成单一数据源。

- 支持渠道：官网表单、广告落地页、展会、社媒、转介绍、Excel 批量导入、开放 API 接入。

- 每条线索自动记录「来源渠道」与「采集时间」。

- 支持 Excel 导入模板下载与字段映射校验。

**业务规则**：

- 导入失败记录需返回错误明细（行号 + 原因）。

- 同渠道重复提交需触发查重提示（见 4.1.4）。

### 4.1.2 线索标签与来源渠道标记（P1）

**功能描述**：为线索打标签、标记来源，便于分类与精细化运营。

- 标签支持多选、自定义新增（如「高意向」「价格敏感」「B2B」）。

- 来源渠道以字典维护，支持排序与启停。

**业务规则**：标签与来源渠道由管理员统一维护，销售仅可选用、不可删除系统内置项。

### 4.1.3 批量筛选、分配、导出（P0）

**功能描述**：对线索进行组合筛选、批量分配负责人、批量导出。

- 筛选维度：来源渠道、标签、状态、负责人、创建时间范围、关键词（姓名/公司）。

- 批量分配：勾选多条线索一次性指定负责人，分配后状态置为「已分配」。

- 导出：按当前筛选条件导出为 Excel，导出字段可配置。

**业务规则**：

- 批量分配支持跨团队分配，操作留痕（谁在何时分配给谁）。

- 导出受数据权限约束，仅导出当前角色可见数据。

### 4.1.4 线索查重（P0）

**功能描述**：新增/导入线索时自动检测重复，避免重复跟进。

- 查重维度：手机号、邮箱（精确匹配）+ 公司名（模糊匹配）。

- 命中时提示疑似重复列表，由用户选择「仍要新增」或「放弃」。

**业务规则**：

- 精确匹配（手机号/邮箱）强制拦截并提示；模糊匹配（公司名）仅提示不拦截。

### 4.1.5 线索回收站（P1）

**功能描述**：删除的线索进入回收站，可恢复或彻底删除。

**业务规则**：

- 回收站线索默认保留 30 天，超期可自动清理（可配置）。

- 回收站中的线索不参与查重、分配与统计。

### 4.1.6 线索状态流转

| 状态  | 说明        | 可流转至         |
| --- | --------- | ------------ |
| 待分配 | 新汇入未指定负责人 | 已分配、已作废      |
| 已分配 | 已指定负责人跟进  | 已转化、已作废、回收站  |
| 已转化 | 已转为客户     | —（终态）        |
| 已作废 | 确认无效      | 回收站          |
| 回收站 | 已删除       | 已分配（恢复）、彻底删除 |

***

## 4.2 客户池

### 4.2.1 客户保护与防撞客（P0）

**功能描述**：锁定已跟进客户，防止多名销售同时跟进同一客户。

**业务规则**：

- 同一客户同时仅允许存在一个有效负责人（owner）。

- 跟进中的客户处于「私有」状态，其他销售不可编辑、不可抢占。

- 撞客冲突由系统按「先绑定者优先」自动裁决，并提示后到者。

### 4.2.2 客户转移与分配（P0）

**功能描述**：支持销售之间客户移交，以及主管的客户分配。

- 主动移交：销售将客户移交给指定同事，需对方确认或主管审批（可配置）。

- 主管分配：主管将客户（含公海客户）分配给团队成员。

- 移交后跟进记录随客户一并转移，历史留痕。

**业务规则**：移交与分配操作全程留痕，可回溯归属变更历史。

### 4.2.3 公海池管理（P0）

**功能描述**：长期未跟进的客户自动掉入公海，供其他销售认领/回收。

- 进入公海：私有客户超过「公海回收阈值」（默认 30 天，可配置）未新增跟进，自动转入公海。

- 认领：销售从公海认领客户，认领后成为该客户负责人，状态转为私有。

- 回收：销售可回收本人曾负责、已掉入公海的客户。

**业务规则**：

- 认领采用并发控制（加锁/乐观锁），同一客户同一时刻仅允许一人认领成功。

- 认领后设置保护期（默认 7 天，可配置），保护期内需产生跟进记录，否则再次掉入公海。

- 关键客户（如高价值商机关联客户）可配置为「不进公海」。

### 4.2.4 客户跟进时效提醒（P0）

**功能描述**：对超期未跟进客户进行提醒，避免客户流失。

**业务规则**：

- 上次跟进后超过「跟进时效阈值」（默认 7 天，可配置）未跟进，触发提醒。

- 提醒方式：站内待办提醒（本期），邮件/企微通知（后续扩展）。

- 提醒对象为当前负责人，抄送其主管。

### 4.2.5 客户状态流转

| 状态 | 说明          | 可流转至         |
| -- | ----------- | ------------ |
| 私有 | 有负责人、跟进中    | 公海（超期未跟进）、锁定 |
| 锁定 | 重点保护，不自动掉公海 | 私有           |
| 公海 | 无人负责、可认领    | 私有（认领/回收）    |

***

## 4.3 客户档案

### 4.3.1 客户建档（P0）

**功能描述**：为客户建立完整档案，存储企业基础资料与联系人信息。

- 企业基础资料：客户名称、行业、规模、地址、官网、统一社会信用代码等。

- 联系人信息：多个联系人，含姓名、职位、电话、邮箱、决策角色。

### 4.3.2 跟进沟通记录沉淀（P0）

**功能描述**：统一沉淀客户的各类沟通记录，形成可回溯的时间线。

- 记录类型：历史邮件、社媒聊天记录、报价记录、电话/面访日志备注。

- 每次跟进可新增日志备注，记录沟通内容、下一步计划、下次跟进时间。

- 支持附件上传。

**业务规则**：跟进记录不可物理删除（仅可修订），保证全生命周期可追溯。

### 4.3.3 全生命周期回溯（P0）

**功能描述**：按时间线查看客户从线索到当前的所有关键节点与跟进记录。

- 时间线聚合展示：线索转化、客户归属变更、跟进日志、报价、商机阶段变化等。

- 支持按记录类型筛选与关键字搜索。

***

## 4.4 商机管理

### 4.4.1 商机新建（P0）

**功能描述**：针对客户的采购咨询创建商机。

- 记录字段：客户、商机名称、客户询盘内容、意向产品、预算、预计成交日期、负责人。

- 商机必须关联客户；一个客户可存在多个商机。

### 4.4.2 商机阶段管理（P0）

**功能描述**：以标准阶段推进商机，阶段变更留痕。

| 阶段   | 说明        |
| ---- | --------- |
| 初步接触 | 建立联系、了解询盘 |
| 报价   | 已发送报价单    |
| 谈判   | 商务谈判、条款磋商 |
| 成交   | 赢单，转为成交   |
| 失败   | 输单，记录失败原因 |

**业务规则**：

- 阶段推进需记录时间与操作人，形成阶段流转日志。

- 「失败」必须填写失败原因；「成交」后进入后续订单流程（范围外）。

- 成交进度以百分比辅助展示（如初步接触 10%、报价 40%、谈判 70%、成交 100%）。

### 4.4.3 商机进度看板（P1）

**功能描述**：以看板（Kanban）形式按阶段分列展示商机。

- 支持按负责人、客户、金额、预计成交日期筛选。

- 支持拖拽改变阶段（触发阶段流转日志）。

- 看板卡片展示：商机名称、客户、预算、负责人、下次跟进时间。

### 4.4.4 商机到期提醒（P0）

**功能描述**：对临近预计成交日期的商机进行提醒。

**业务规则**：

- 距「预计成交日期」小于提醒阈值（默认 3 天，可配置）且未成交/失败，触发提醒。

- 逾期未关闭的商机持续提醒，直至关闭或更新预计成交日期。

***

## 4.5 日程管理

### 4.5.1 每日工作日报（P0）

**功能描述**：销售填写每日工作日报，记录当日工作内容与成果。

- 日报字段：日期、工作内容、拜访/沟通客户数、问题与计划。

### 4.5.2 拓客任务（P1）

**功能描述**：创建拓客任务，管理每日拓展客户的目标与执行。

- 任务可关联线索/客户，记录完成状态。

### 4.5.3 日历待办事项（P0）

**功能描述**：以日历视图创建与管理待办事项。

- 支持月/周/日视图切换；待办关联线索/客户/商机。

- 待办字段：标题、时间、关联对象、提醒时间、完成状态。

### 4.5.4 团队工作计划与进度查看（P0）

**功能描述**：管理者查看团队成员的日报、待办与完成进度。

- 主管视图：按成员/日期聚合展示工作计划与完成率。

- 完成率 = 已完成待办数 / 应完成待办数。

### 4.5.5 任务提醒（P1）

**功能描述**：对待办、日报、拓客任务按设定时间提醒。

**业务规则**：提醒方式为站内待办提醒（本期），邮件/企微后续扩展。

***

## 5. 核心数据对象

| 实体               | 关键字段                                  |
| ---------------- | ------------------------------------- |
| 线索 Lead          | 姓名、公司、电话、邮箱、来源渠道、标签、状态、负责人、备注、创建/更新时间 |
| 客户 Customer      | 客户名称、行业、电话、邮箱、归属状态、负责人、最近跟进时间、备注      |
| 联系人 Contact      | 所属客户、姓名、职位、电话、邮箱、决策角色                 |
| 跟进记录 FollowUpLog | 关联客户/商机、记录类型、内容、附件、操作人、时间             |
| 商机 Opportunity   | 关联客户、商机名称、询盘、意向产品、预算、阶段、预计成交日期、负责人    |
| 任务/日报 Task       | 标题、类型（日报/待办/拓客）、内容、负责人、关联对象、截止时间、完成状态 |

***

## 6. 关键业务规则汇总

| 规则            | 默认值    | 是否可配置 |
| ------------- | ------ | ----- |
| 公海回收阈值（未跟进天数） | 30 天   | 是     |
| 认领保护期         | 7 天    | 是     |
| 跟进时效提醒阈值      | 7 天    | 是     |
| 商机到期提醒阈值      | 提前 3 天 | 是     |
| 线索查重（精确）      | 手机号/邮箱 | 是     |
| 线索查重（模糊）      | 公司名    | 是     |
| 回收站保留时长       | 30 天   | 是     |

***

## 7. 非功能需求

| 类别   | 要求                           |
| ---- | ---------------------------- |
| 性能   | 常规列表页查询响应 ≤ 2s；单次导出 ≤ 5000 条 |
| 安全   | 密码加盐哈希存储；接口鉴权（JWT）；操作日志留痕    |
| 数据权限 | 按负责人/团队/公海三层隔离，越权访问拦截        |
| 可用性  | 核心模块（线索/客户/商机）可用性 ≥ 99.5%    |
| 兼容性  | 支持 Chrome、Edge 最新两个主版本       |
| 并发   | 公海认领、批量分配支持并发安全              |

***

## 8. 验收标准（摘要）

1. 线索可从至少 5 类渠道统一汇入，并完成查重、标签、批量分配与导出。
2. 客户归属唯一，撞客被系统自动拦截；超期未跟进客户按规则进入公海并可被认领。
3. 客户档案可完整记录资料、联系人、邮件/社媒/报价/日志，并支持时间线回溯。
4. 商机可按「初步接触 → 报价 → 谈判 → 成交/失败」推进，看板与到期提醒生效。
5. 销售可填写日报与待办，主管可查看团队计划与完成进度，提醒按时触发。

